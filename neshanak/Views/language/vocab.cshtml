@model neshanak.viewModel.vocabVM
@{
    ViewBag.Title = "GetSubCat";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section styles{


    <link href="~/Content/Site.css" rel="stylesheet" />
    <link href="~/Content/fontawesome-all.css" rel="stylesheet" />

    <link href="~/Content/slick.css" rel="stylesheet" />
    <link href="~/Content/slick-theme.css" rel="stylesheet" />
}
@section scripts{
    <script src="~/Scripts/jquery.migrate.js"></script>
    <script src="~/Scripts/slick.js"></script>

    <script>
        $(document).ready(function () {
            $('.your-class').slick({
                dots: false,
                infinite: true,
                autoplay: true,
                speed: 500,
                cssEase: 'linear'
            });
        });
    </script>






    <link href="~/Content/slider.css" rel="stylesheet" />
    <script src="~/Scripts/sliderscript.js"></script>
    <script>
        $('.bo-slider').boSlider({
            slideShow: true,
            interval: 8000
        });
    </script>

}





@if (Model.sld != null)
{
    <ul class="bo-slider">
        @foreach (var slid in Model.sld)
        {
            if (slid.type == "1")
            {
                <li data-url="@slid.image" data-type="image"></li>
            }
            else
            {
                string videopath = "/video/" + @Path.GetFileNameWithoutExtension(slid.image) + ".mp4";
                <li data-url="@videopath" data-type="video"></li>
            }
        }


    </ul>

}
@*@if (Model.sld != null)
    {
        <div class="row" style="background-color:black">
            <div id="slideHolder" style=" width:100%; height:auto; margin:auto;display:block">
                <div class="your-class">
                    @foreach (var slid in Model.sld)
                    {
                        <div class="imageholder">
                            <img src="@slid.image" />
                        </div>
                    }




                </div>
            </div>
        </div>
    }*@


<style>
    .learningP {
        border: 0 !important;
    }
    .itemitself{
        display:flex
    }

    .itemitself:hover {
        border-radius: 8px;
        box-shadow: 2px 2px 5px -1px rgb(148 149 153);
    }
        .itemitself{
            display:flex;
        }
        @@media screen and (max-width: 750px) {
            .itemitself{
                display:block
            }
    }
    
</style>
<!-- #endregion Jssor Slider End -->
<div style="" class="subcatHolder" data-slide="1" data-speed="500">
    <div>
        @foreach (var item in Model.VocabList)
        {
            <div class="final catitem" style="width:100%">
                <a style="text-decoration:none" href="/language/vocab?lessonID=@item.id">
                    <div class="itemitself">


                        <p class="learningP " style="direction:ltr; margin:20px 5px; font-weight:500; font-size:14px;padding-top:0">@item.title</p>
                        <p class="learningP " style="margin:20px 5px; font-weight:500; font-size:14px;padding-top:0">@item.pronun</p>
                        <p class="learningP " style="margin:20px 5px; font-weight:500; font-size:14px;padding-top:0">@item.translation</p>
                    </div>
                </a>

            </div>
        }

    </div>
    @*<img class="bannerItem" src="http://supectco.com/app/portal/uploads/@Model.bnr.First().img" />*@
</div>
